<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CampReady" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorPrimaryVariant">@color/green_dark</item>
        <item name="colorSecondary">@color/orange_accent</item>
        <item name="android:windowBackground">@color/cream_background</item>
    </style>

    <style name="Theme.CampReady" parent="Base.Theme.CampReady" />
</resources>